name: Run Lint and Test

on:
  push:
    branches: [ "*" ]

jobs:

  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.24'

      - name: Install
        run: make install

      - name: Lint
        run: make lint

      - name: Test
        run: make test
